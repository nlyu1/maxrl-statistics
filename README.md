# MaxRL-statistics

Research code for maximum-likelihood reinforcement learning for statistical estimation.

## Layout

- `src/`: shared library code — see [`src/README.md`](src/README.md).
- `experiments/`: runnable experiment scripts, sweeps, and notebooks.
- `artifacts/`: local cache of sweep outputs, checkpoints, and figures.
- `theory/`: markdown notes backing the writeup.
- `writeup/`: Quarto source (`quarto preview` from inside).
- `docs/`: autogenerated writeup artifacts.

## Setup

1. Install [uv](https://docs.astral.sh/uv/getting-started/installation/) and run `uv sync --dev`.
2. Create `.env` at repo root: `REPO_BASE="/absolute/path/to/maxrl-statistics"`.
