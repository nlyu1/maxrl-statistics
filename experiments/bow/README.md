# Bag-of-words experiments

This document includes instructions to run / replicate the experiments.
The `single_run.py` scripts runs a single experiment. The `orchestrate.py`
runs experiments via hard-coding shards across two GPUs.